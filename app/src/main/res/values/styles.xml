<resources>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:background">@drawable/splash_screen</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Dummy theme for transparent nav button background, see v21 -->
    <style name="AppTheme.TransNav" parent="AppTheme"/>

    <style name="TabFragment">
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:paddingBottom">@dimen/bottom_nav_height</item>
    </style>

    <style name="PageHeader">
        <item name="fontPath">fonts/BungeeInline.ttf</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">@dimen/page_title_text_size</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="HugeText">
        <item name="android:textSize">@dimen/huge_text_size</item>
        <item name="android:textColor">@android:color/white</item>
    </style>
</resources>
